# Dependencies
node_modules/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Build
*.zip

# Screenshots (generated, large)
test/screenshots/

# Prompt engineering / IP
# brain-widget-schemas.json is the only private asset (excluded from the shipped
# zip by build-zip.sh). The shipped runtime assets (brain.json, config schemas,
# system prompt) are tracked in git because they're public on WP.org SVN and
# required at runtime — gitignoring them broke clean-checkout builds.
brain-widget-schemas.json

# Environment
.env

# SVN credentials
.svn-credentials

# Local Claude session config (per-user, not for repo)
.claude/
