# Files to exclude from the distributed plugin zip.
# Kept in sync with build-zip.sh's exclude list (the canonical one). Nothing in
# this repo consumes .distignore today; it exists for wp-cli dist-archive
# compatibility.
.distignore
.gitignore
.git
node_modules
test
package.json
package-lock.json
CLAUDE.md
brain-widget-schemas.json
*.mjs
